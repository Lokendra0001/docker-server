version: "3.8"

services:
  backend:
    build: . # Build from current folder
    ports:
      - "5001:5001" # Map host port 5001 to container
    environment:
      - NODE_ENV=development
      - PORT=5001
