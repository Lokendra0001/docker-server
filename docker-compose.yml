version: "3.8"

services:
  backend:
    build: . # Build from current folder
    ports:
      - "5000:5000" # Map host port 5000 to container
    environment:
      - NODE_ENV=development
      - PORT=5000
